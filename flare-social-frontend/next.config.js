/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i.pravatar.cc',
      'pbs.twimg.com',
    ],
  },
  reactStrictMode: true,
}

module.exports = nextConfig 